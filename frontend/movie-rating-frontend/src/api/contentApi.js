import { get } from \"./apiClient\";\nimport { mapMovieDtoToCard, mapShowDtoToCard } from \"../mappers/contentMappers\";\n\nconst extractList = (payload) => {\n  if (Array.isArray(payload)) return payload;\n  if (payload?.data && Array.isArray(payload.data)) return payload.data;\n  if (payload?.items && Array.isArray(payload.items)) return payload.items;\n  if (payload?.results && Array.isArray(payload.results)) return payload.results;\n  return [];\n};\n\n/**\n * Fetches Top Rated Movies\n */\nexport const fetchTopRatedMovies = async () => {\n  const res = await get(\"/api/Movie/top-rated\");\n  return extractList(res).map((item, idx) => mapMovieDtoToCard(item, idx));\n};\n\n/**\n * Fetches Top Rated Shows\n */\nexport const fetchTopRatedShows = async () => {\n  const res = await get(\"/api/Show/top-rated\");\n  return extractList(res).map((item, idx) => mapShowDtoToCard(item, idx));\n};\n\n/**\n * Fetches all Movies (can be used for generic \"Trending\" or base browsing)\n */\nexport const fetchMovies = async () => {\n  const res = await get(\"/api/Movie\");\n  return extractList(res).map((item, idx) => mapMovieDtoToCard(item, idx));\n};\n\n/**\n * Fetches all Shows\n */\nexport const fetchShows = async () => {\n  const res = await get(\"/api/Show\");\n  return extractList(res).map((item, idx) => mapShowDtoToCard(item, idx));\n};\n\n/**\n * Fetches a single Movie by ID\n */\nexport const fetchMovieById = async (id) => {\n  const res = await get(`/api/Movie/${id}`);\n  return mapMovieDtoToCard(res);\n};\n\n/**\n * Fetches a single Show by ID\n */\nexport const fetchShowById = async (id) => {\n  const res = await get(`/api/Show/${id}`);\n  return mapShowDtoToCard(res);\n};\n