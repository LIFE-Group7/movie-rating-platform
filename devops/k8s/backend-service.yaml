apiVersion: v1
kind: Service
metadata:
  name: backend-service
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-resource-group: "MC_Movie-Rating_aks-movie-rating_austriaeast"
spec:
  type: LoadBalancer
  loadBalancerIP: 68.210.96.73
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
